apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubeflux
  namespace: scheduler-plugins
spec:
  replicas: 1
  selector:
    matchLabels:
      component: scheduler
  template:
    metadata:
      labels:
        component: scheduler
    spec:
      serviceAccountName: scheduler-plugins
      containers:
        - image: quay.io/cmisale1/kubeflux-sidecar:latest
          imagePullPolicy: Always
          command:
          - /go/src/kubeflux/bin/server
          - --policy=lonode
          name: kubeflux-sidecar
        - image: quay.io/cmisale1/kubeflux:dev
          imagePullPolicy: Always
          command:
          - /bin/kube-scheduler
          - --config=/etc/kubernetes/scheduler-config.yaml
          - -v=9
          name: kubeflux
          resources:
            requests:
              cpu: '0.1'
          securityContext:
            privileged: false
          volumeMounts:
          - mountPath: /etc/kubernetes
            name: scheduler-config
      hostNetwork: false
      hostPID: false
      volumes:
      - name: scheduler-config
        configMap:
          name: scheduler-config
        
